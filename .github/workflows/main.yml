name: Amazon Affiliate Auto Poster

on:
  schedule:
    # Din mein 2 baar chalega: UTC 02:00 aur 14:00 
    - cron: '0 2,14 * * *'
  workflow_dispatch: # Manual run ke liye button dega

permissions:
  contents: write # Repository mein wapas commit karne ki permission

jobs:
  scrape-and-post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run Scraper Script
        env:
          # Aapke saare secrets jo Settings > Secrets and variables mein hain
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python scraper.py

      - name: Commit Updated History File
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Agar history.json mein koi naya data aya hai, toh use commit kar do
          if [[ -n $(git status --porcelain history.json) ]]; then
            git add history.json
            git commit -m "Auto-update history.json with latest posted link timestamp"
            git push
          else
            echo "Koi naya link post nahi hua, history file same hai."
          fi
